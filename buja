Backend (Laravel)

Laravel backend created successfully

MySQL database nepal_cozy_care connected via .env

Default migrations executed successfully

Laravel Sanctum installed and configured

personal_access_tokens table created

User model updated with HasApiTokens

Role-based system added (customer, admin)

/api/register endpoint tested via POST request

User successfully stored in database

Sanctum token generated and returned

Tools Used

Laravel 12

PHP 8.2

MySQL (XAMPP)

Thunder Client for API testing

React frontend (already connected)

🧠 What this means technically

Your backend is now a secure REST API

Frontend can authenticate users using Bearer tokens

You are following industry-standard architecture

This is FYP-level correct implementation

🚀 What you should do NEXT (recommended order)
🔹 Step 1 (Next logical step) – Login API

You already did register. Now:

Create /api/login

Return token on login

Test login in Thunder Client

👉 This completes Auth Module

🔹 Step 2 – Protect routes using Sanctum

Make some routes only accessible to logged-in users

Later, admin-only routes

🔹 Step 3 – Plant Module (Core feature)

plants table migration

Plant model

APIs:

list plants

view plant

admin add/edit/delete plant

🔹 Step 4 – Connect React frontend

Login page → backend

Store token

Fetch protected data

🎓 What you can confidently say in Viva

“The backend authentication system is implemented using Laravel Sanctum. User registration and login APIs were tested using POST requests, and secure token-based authentication is used to protect routes.”