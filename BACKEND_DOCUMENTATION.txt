================================================================================
                    NEPAL COZY CARE - BACKEND DOCUMENTATION
                         Plant E-Commerce API (Laravel)
================================================================================

Project: Nepal Cozy Care
Framework: Laravel 11
Database: MySQL
Authentication: Laravel Sanctum (Token-based)
Created: January 2026

================================================================================
                              TABLE OF CONTENTS
================================================================================

1. PROJECT STRUCTURE
2. DATABASE TABLES
3. AUTHENTICATION SYSTEM
4. PLANT MANAGEMENT
5. CART SYSTEM
6. ORDER SYSTEM
7. MIDDLEWARE
8. API ENDPOINTS SUMMARY

================================================================================
                          1. PROJECT STRUCTURE
================================================================================

Nepal-Cozy-Care-backend/
├── app/
│   ├── Http/
│   │   ├── Controllers/
│   │   │   └── Api/
│   │   │       ├── AuthController.php      → User authentication
│   │   │       ├── PlantController.php     → Plant CRUD operations
│   │   │       ├── CartController.php      → Shopping cart management
│   │   │       └── OrderController.php     → Order & checkout
│   │   └── Middleware/
│   │       └── AdminMiddleware.php         → Admin-only route protection
│   ├── Models/
│   │   ├── User.php                        → User model with role
│   │   ├── Plant.php                       → Plant model
│   │   ├── Cart.php                        → Cart model
│   │   ├── Order.php                       → Order model
│   │   └── OrderItem.php                   → Order items model
├── database/
│   └── migrations/
│       ├── create_users_table.php
│       ├── add_role_to_users_table.php
│       ├── create_plants_table.php
│       ├── create_carts_table.php
│       ├── create_orders_table.php
│       └── create_order_items_table.php
├── routes/
│   └── api.php                             → All API routes
└── bootstrap/
    └── app.php                             → Middleware registration

================================================================================
                          2. DATABASE TABLES
================================================================================

--------------------------------------------------------------------------------
TABLE: users
--------------------------------------------------------------------------------
Purpose: Store user accounts (customers and admins)

Columns:
- id                  : Primary key (auto-increment)
- name                : User's full name (string)
- email               : User's email (unique)
- password            : Hashed password
- role                : 'customer' or 'admin' (default: customer)
- email_verified_at   : Email verification timestamp (nullable)
- remember_token      : For "remember me" functionality
- created_at          : Record creation timestamp
- updated_at          : Record update timestamp

--------------------------------------------------------------------------------
TABLE: plants
--------------------------------------------------------------------------------
Purpose: Store plant products for the shop

Columns:
- id                  : Primary key (auto-increment)
- name                : Plant name (required)
- scientific_name     : Scientific/botanical name (nullable)
- category            : Category like Indoor, Outdoor, etc. (nullable)
- difficulty          : Care difficulty - Easy, Medium, Hard (nullable)
- light               : Light requirements (nullable)
- water               : Watering frequency (nullable)
- soil                : Soil type requirements (nullable)
- description         : Detailed plant description (nullable)
- price               : Price in NPR (decimal, required)
- stock               : Available quantity (integer, required)
- image               : Image URL/path (nullable)
- is_active           : Whether plant is available for sale (boolean, default: true)
- created_at          : Record creation timestamp
- updated_at          : Record update timestamp

--------------------------------------------------------------------------------
TABLE: carts
--------------------------------------------------------------------------------
Purpose: Store shopping cart items for each user

Columns:
- id                  : Primary key (auto-increment)
- user_id             : Foreign key to users table (cascade on delete)
- plant_id            : Foreign key to plants table (cascade on delete)
- quantity            : Number of items (integer, default: 1)
- created_at          : Record creation timestamp
- updated_at          : Record update timestamp

Constraints:
- UNIQUE(user_id, plant_id) : Prevents duplicate items, updates quantity instead

--------------------------------------------------------------------------------
TABLE: orders
--------------------------------------------------------------------------------
Purpose: Store customer orders after checkout

Columns:
- id                  : Primary key (auto-increment)
- user_id             : Foreign key to users table (cascade on delete)
- status              : Order status (default: 'pending')
                        Options: pending, processing, shipped, delivered, cancelled
- payment_status      : Payment state (default: 'unpaid')
                        Options: unpaid, paid, failed, refunded
- subtotal            : Sum of all items before fees (decimal)
- delivery_fee        : Delivery/shipping charge (decimal, default: 0)
- tax                 : Tax amount (decimal, default: 0)
- total               : Final total amount (decimal)
- shipping_name       : Recipient name (nullable)
- shipping_phone      : Contact phone number (nullable)
- shipping_address    : Delivery address (nullable)
- created_at          : Record creation timestamp
- updated_at          : Record update timestamp

--------------------------------------------------------------------------------
TABLE: order_items
--------------------------------------------------------------------------------
Purpose: Store individual items within each order

Columns:
- id                  : Primary key (auto-increment)
- order_id            : Foreign key to orders table (cascade on delete)
- plant_id            : Foreign key to plants table (cascade on delete)
- quantity            : Number of items ordered (integer)
- price               : Price at time of order (decimal) - SNAPSHOT
- line_total          : quantity × price (decimal)
- created_at          : Record creation timestamp
- updated_at          : Record update timestamp

Note: Price is stored as a snapshot to preserve historical order data
      even if the plant price changes later.

--------------------------------------------------------------------------------
TABLE: wishlists
--------------------------------------------------------------------------------
Purpose: Store plants that users have saved for later.

Columns:
- id                  : Primary key (auto-increment)
- user_id             : Foreign key to users table (cascade on delete)
- plant_id            : Foreign key to plants table (cascade on delete)
- created_at          : Record creation timestamp
- updated_at          : Record update timestamp

Constraints:
- UNIQUE(user_id, plant_id) : One wishlist entry per user per plant

--------------------------------------------------------------------------------
TABLE: reviews
--------------------------------------------------------------------------------
Purpose: Store user reviews and ratings for plants.

Columns:
- id                  : Primary key (auto-increment)
- user_id             : Foreign key to users table (cascade on delete)
- plant_id            : Foreign key to plants table (cascade on delete)
- rating              : Rating from 1 to 5 (integer)
- comment             : Optional text feedback
- created_at          : Record creation timestamp
- updated_at          : Record update timestamp

Constraints:
- UNIQUE(user_id, plant_id) : One review per user per plant

--------------------------------------------------------------------------------
TABLE: blogs
--------------------------------------------------------------------------------
Purpose: Store gardening tips and blog articles.

Columns:
- id                  : Primary key (auto-increment)
- user_id             : Optional author (admin user)
- title               : Article title
- slug                : URL-friendly unique identifier
- excerpt             : Short summary (optional)
- content             : Full article content
- image               : Optional image URL/path
- is_published        : Whether article is visible publicly
- published_at        : When the article was published
- created_at          : Record creation timestamp
- updated_at          : Record update timestamp

================================================================================
                       3. AUTHENTICATION SYSTEM
================================================================================

File: app/Http/Controllers/Api/AuthController.php

--------------------------------------------------------------------------------
REGISTER - POST /api/register
--------------------------------------------------------------------------------
Purpose: Create a new user account

Request Body:
{
    "name": "string (required)",
    "email": "string (required, unique)",
    "password": "string (required, min: 6 characters)",
    "password_confirmation": "string (required, must match password)"
}

Response (201 Created):
{
    "message": "User registered successfully",
    "user": { user object },
    "token": "authentication token"
}

Process:
1. Validate input data
2. Hash the password
3. Create user with role 'customer'
4. Generate Sanctum token
5. Return user data and token

--------------------------------------------------------------------------------
LOGIN - POST /api/login
--------------------------------------------------------------------------------
Purpose: Authenticate existing user

Request Body:
{
    "email": "string (required)",
    "password": "string (required)"
}

Response (200 OK):
{
    "message": "Login successful",
    "user": { user object },
    "token": "authentication token"
}

Response (401 Unauthorized):
{
    "message": "Invalid credentials"
}

Process:
1. Validate input data
2. Find user by email
3. Verify password using Hash::check()
4. Generate new Sanctum token
5. Return user data and token

--------------------------------------------------------------------------------
LOGOUT - POST /api/logout (Requires Auth)
--------------------------------------------------------------------------------
Purpose: Invalidate current authentication token

Headers Required:
Authorization: Bearer {token}

Response (200 OK):
{
    "message": "Logged out successfully"
}

Process:
1. Get current authenticated user
2. Delete current access token
3. Return success message

--------------------------------------------------------------------------------
GET PROFILE - GET /api/me (Requires Auth)
--------------------------------------------------------------------------------
Purpose: Get current logged-in user's information

Headers Required:
Authorization: Bearer {token}

Response (200 OK):
{
    "user": { user object }
}

================================================================================
                        4. PLANT MANAGEMENT
================================================================================

File: app/Http/Controllers/Api/PlantController.php

Public plant listing and detail endpoints allow browsing without a token.
Admin-only endpoints for creating and updating plants still require auth.

--------------------------------------------------------------------------------
LIST PLANTS - GET /api/plants
--------------------------------------------------------------------------------
Purpose: Get all active plants with filters, sorting, pagination and rating info.

Response (200 OK) example:
{
    "message": null,
    "data": {
        "plants": [
            {
                "id": 1,
                "name": "Aloe Vera",
                "category": "Indoor",
                "price": 300,
                "stock": 15,
                "avg_rating": 4.5,
                "review_count": 3,
                ...
            }
        ],
        "pagination": {
            "current_page": 1,
            "per_page": 12,
            "total": 20,
            "last_page": 2
        }
    }
}

Process:
1. Query plants where is_active = true.
2. Apply optional filters (category, difficulty, light, water, price range, search).
3. Apply sorting (price, name, newest).
4. Use withAvg and withCount to compute rating summary.
5. Paginate results.

--------------------------------------------------------------------------------
SHOW PLANT - GET /api/plants/{id}
--------------------------------------------------------------------------------
Purpose: Get single plant details with rating summary.

Response (200 OK) example:
{
    "message": null,
    "data": {
        "plant": {
            "id": 1,
            "name": "Aloe Vera",
            "avg_rating": 4.5,
            "review_count": 3,
            ...
        }
    }
}

Response (404 Not Found):
If plant ID doesn't exist or is inactive.

--------------------------------------------------------------------------------
CREATE PLANT - POST /api/plants (Requires Auth)
--------------------------------------------------------------------------------
Purpose: Add a new plant to the shop

Request Body:
{
    "name": "string (required)",
    "scientific_name": "string (optional)",
    "category": "string (optional)",
    "difficulty": "string (optional)",
    "light": "string (optional)",
    "water": "string (optional)",
    "soil": "string (optional)",
    "description": "string (optional)",
    "price": "number (required, min: 0)",
    "stock": "integer (required, min: 0)",
    "image": "string (optional)",
    "is_active": "boolean (optional, default: true)"
}

Response (201 Created):
{
    "message": "Plant added successfully",
    "plant": { created plant object }
}

--------------------------------------------------------------------------------
UPDATE PLANT - PUT /api/plants/{id} (Requires Auth)
--------------------------------------------------------------------------------
Purpose: Update existing plant information

Request Body: Same as create (all fields optional with 'sometimes' rule)

Response (200 OK):
{
    "message": "Plant updated successfully",
    "plant": { updated plant object }
}

--------------------------------------------------------------------------------
DELETE PLANT - DELETE /api/plants/{id} (Requires Auth)
--------------------------------------------------------------------------------
Purpose: Remove a plant from the database

Response (200 OK):
{
    "message": "Plant deleted successfully"
}

================================================================================
                          5. CART SYSTEM
================================================================================

File: app/Http/Controllers/Api/CartController.php

All cart routes require authentication (auth:sanctum middleware)
Each user has their own cart - users can only see/modify their own cart items

--------------------------------------------------------------------------------
VIEW CART - GET /api/cart (Requires Auth)
--------------------------------------------------------------------------------
Purpose: Get current user's cart with all items and total

Response (200 OK):
{
    "message": null,
    "data": {
        "cart": [
            {
                "id": 1,
                "user_id": 1,
                "plant_id": 5,
                "quantity": 2,
                "plant": { plant object }
            }
        ],
        "total": 600
    }
}

Process:
1. Get cart items for current user
2. Load plant relationship
3. Calculate total (sum of price × quantity)
4. Return cart items and total

--------------------------------------------------------------------------------
ADD TO CART - POST /api/cart (Requires Auth)
--------------------------------------------------------------------------------
Purpose: Add a plant to the cart

Request Body:
{
    "plant_id": "integer (required, must exist in plants table)",
    "quantity": "integer (optional, default: 1, min: 1)"
}

Response (201 Created):
{
    "message": "Added to cart",
    "data": {
        "cart": { cart item with plant }
    }
}

Response (400 Bad Request):
{
    "message": "Not enough stock",
    "errors": {
        "quantity": ["Not enough stock"]
    }
}

Process:
1. Validate plant exists
2. Check if enough stock available
3. If item already in cart → increase quantity
4. If new item → create cart entry
5. Validate total quantity against stock
6. Return updated cart item

--------------------------------------------------------------------------------
UPDATE QUANTITY - PUT /api/cart/{id} (Requires Auth)
--------------------------------------------------------------------------------
Purpose: Change quantity of a cart item

Request Body:
{
    "quantity": "integer (required, min: 1)"
}

Response (200 OK):
{
    "message": "Cart updated",
    "data": {
        "cart": { updated cart item }
    }
}

Response (400 Bad Request):
{
    "message": "Not enough stock",
    "errors": {
        "quantity": ["Not enough stock"]
    }
}

Process:
1. Find cart item (must belong to current user)
2. Check stock availability
3. Update quantity
4. Return updated cart item

--------------------------------------------------------------------------------
REMOVE ITEM - DELETE /api/cart/{id} (Requires Auth)
--------------------------------------------------------------------------------
Purpose: Remove a single item from cart

Response (200 OK):
{
    "message": "Removed from cart",
    "data": null
}

--------------------------------------------------------------------------------
CLEAR CART - DELETE /api/cart (Requires Auth)
--------------------------------------------------------------------------------
Purpose: Remove all items from user's cart

Response (200 OK):
{
    "message": "Cart cleared",
    "data": null
}

================================================================================
                          6. ORDER SYSTEM
================================================================================

File: app/Http/Controllers/Api/OrderController.php

--------------------------------------------------------------------------------
CHECKOUT - POST /api/checkout (Requires Auth)
--------------------------------------------------------------------------------
Purpose: Convert cart to an order

Request Body:
{
    "shipping_name": "string (optional)",
    "shipping_phone": "string (optional)",
    "shipping_address": "string (optional)"
}

Response (201 Created):
{
    "message": "Order placed successfully",
    "data": {
        "order": {
        "id": 1,
        "user_id": 1,
        "status": "pending",
        "subtotal": 600,
        "delivery_fee": 0,
        "tax": 0,
        "total": 600,
        "shipping_name": "John Doe",
        "shipping_phone": "9841234567",
        "shipping_address": "Kathmandu, Nepal",
        "items": [
            {
                "id": 1,
                "plant_id": 5,
                "quantity": 2,
                "price": 300,
                "line_total": 600,
                "plant": { plant object }
            }
        ]
    }
}

Response (400 Bad Request):
{
    "message": "Cart is empty",
    "errors": {
        "cart": ["Cart is empty"]
    }
}
OR
{
    "message": "Not enough stock for {plant name}",
    "available_stock": 5
}

Process (within database transaction):
1. Get user's cart items
2. Validate cart is not empty
3. Check stock availability for each item
4. Calculate subtotal
5. Create order record
6. Create order items (with price snapshot)
7. Reduce stock for each plant
8. Clear user's cart
9. Return order with items

--------------------------------------------------------------------------------
MY ORDERS - GET /api/orders (Requires Auth)
--------------------------------------------------------------------------------
Purpose: Get all orders for current user (paginated).

Response (200 OK) example:
{
    "message": null,
    "data": {
        "orders": [
            {
                "id": 1,
                "status": "delivered",
                "payment_status": "paid",
                "total": 600,
                "items": [...]
            }
        ],
        "pagination": {
            "current_page": 1,
            "per_page": 10,
            "total": 5,
            "last_page": 1
        }
    }
}

--------------------------------------------------------------------------------
ORDER DETAILS - GET /api/orders/{id} (Requires Auth)
--------------------------------------------------------------------------------
Purpose: Get single order details (owner only).

Response (200 OK):
{
    "message": null,
    "data": {
        "order": { order object with items }
    }
}

Response (403 Forbidden):
{
    "message": "Forbidden",
    "errors": {}
}
(If trying to view another user's order)

--------------------------------------------------------------------------------
UPDATE STATUS - PUT /api/orders/{id}/status (Admin Only)
--------------------------------------------------------------------------------
Purpose: Update order status (admin only).

Request Body:
{
    "status": "string (required)"
    // Options: pending, processing, shipped, delivered, cancelled
}

Response (200 OK):
{
    "message": "Order status updated",
    "data": {
        "order": { updated order }
    }
}

Response (403 Forbidden):
{
    "message": "Forbidden. Admin access required."
}
(If user is not admin)

--------------------------------------------------------------------------------
CANCEL ORDER - POST /api/orders/{id}/cancel (Requires Auth)
--------------------------------------------------------------------------------
Purpose: Cancel a pending order and restore stock.

Rules:
- Only pending orders can be cancelled.
- Customers can cancel only their own orders.
- Admins can cancel any pending order.

Response (200 OK):
{
    "message": "Order cancelled successfully",
    "data": {
        "order": { cancelled order with items }
    }
}

Response (400 Bad Request):
{
    "message": "Only pending orders can be cancelled",
    "errors": {}
}

Response (403 Forbidden):
{
    "message": "Forbidden",
    "errors": {}
}

================================================================================
                           7. MIDDLEWARE
================================================================================

--------------------------------------------------------------------------------
AUTH:SANCTUM (Laravel Built-in)
--------------------------------------------------------------------------------
File: Provided by Laravel Sanctum package

Purpose: Protect routes that require authentication

Usage in routes:
Route::middleware('auth:sanctum')->group(function () {
    // Protected routes here
});

Behavior:
- Checks for valid Bearer token in Authorization header
- Returns 401 if token is missing or invalid

--------------------------------------------------------------------------------
ADMIN MIDDLEWARE (Custom)
--------------------------------------------------------------------------------
File: app/Http/Middleware/AdminMiddleware.php

Purpose: Restrict routes to admin users only

Registration: bootstrap/app.php
$middleware->alias([
    'admin' => \App\Http\Middleware\AdminMiddleware::class,
]);

Usage in routes:
Route::middleware(['auth:sanctum', 'admin'])->group(function () {
    // Admin-only routes here
});

Behavior:
- Checks if authenticated user has role = 'admin'
- Returns 403 Forbidden if user is not admin

================================================================================
                       8. API ENDPOINTS SUMMARY
================================================================================

BASE URL: http://127.0.0.1:8000/api

--------------------------------------------------------------------------------
PUBLIC ENDPOINTS (No Auth Required)
--------------------------------------------------------------------------------
GET    /ping                      → Test API connection
POST   /register                  → Create new user account
POST   /login                     → Login and get token

GET    /plants                    → List all active plants (filters + pagination)
GET    /plants/{id}               → Get single plant with rating summary
GET    /plants/{id}/reviews       → List reviews for an active plant

GET    /blogs                     → List published blog articles
GET    /blogs/{id}                → View single blog article

--------------------------------------------------------------------------------
AUTHENTICATED ENDPOINTS (Requires Bearer Token)
--------------------------------------------------------------------------------
POST   /logout                    → Logout (invalidate token)
GET    /me                        → Get current user profile

// Plants (usually admin-managed)
POST   /plants                    → Create new plant
PUT    /plants/{id}               → Update plant
DELETE /plants/{id}               → Delete plant

// Cart
GET    /cart                      → View cart with total
POST   /cart                      → Add item to cart
PUT    /cart/{id}                 → Update cart item quantity
DELETE /cart/{id}                 → Remove item from cart
DELETE /cart                      → Clear entire cart

// Orders
POST   /checkout                  → Create order from cart
GET    /orders                    → List my orders (paginated)
GET    /orders/{id}               → View order details
POST   /orders/{id}/cancel        → Cancel my own pending order

// Wishlist
GET    /wishlist                  → List my wishlist
POST   /wishlist                  → Add plant to wishlist
DELETE /wishlist/{plantId}        → Remove plant from wishlist

// Reviews
POST   /reviews                   → Create or update a review

--------------------------------------------------------------------------------
ADMIN ONLY ENDPOINTS (Requires Admin Role)
--------------------------------------------------------------------------------
GET    /admin/orders              → List all orders with pagination
PUT    /orders/{id}/status        → Update order status
POST   /blogs                     → Create blog article
PUT    /blogs/{id}                → Update blog article
DELETE /blogs/{id}                → Delete blog article

================================================================================
                         AUTHENTICATION HEADER
================================================================================

For all protected routes, include this header:

Authorization: Bearer {your_token_here}

Example:
Authorization: Bearer 5|d6kjPjOtcDCWPDhn2iaq6leU5OFXURmOnSL8f5cI54e81c0a

================================================================================
                           ORDER STATUSES
================================================================================

1. pending     → Order placed, waiting for processing
2. processing  → Order is being prepared
3. shipped     → Order has been shipped
4. delivered   → Order delivered to customer
5. cancelled   → Order was cancelled

================================================================================
                            USER ROLES
================================================================================

1. customer (default) → Can browse, cart, order, view own orders
2. admin              → Can do everything + update order statuses

================================================================================
                          END OF DOCUMENTATION
================================================================================

Last Updated: January 14, 2026
